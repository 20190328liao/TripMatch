@{
    ViewData["Title"] = "忘記帳號";
    Layout = "~/Views/Shared/_LoginLayout.cshtml";
}
@section Styles {
    <link rel="stylesheet" href="~/css/AuthApi/forgot.css" asp-append-version="true" />
}
<div class="forgotLineWrap">
    <h1 class="forgotLine loginH1 titleWrap titleH1">忘記帳號</h1>
</div>
<div class="forgot_wrap">

    <!-- 步驟指示器：起始皆未完成 -->
    <div class="step_indicators mb-5">
        <div class="step_item step_incomplete" id="step1_indicator">
            <div class="step_badge"><span class="step_number">1</span></div>
            <div class="step_label">驗證帳號</div>
        </div>
        <div class="step_item step_incomplete" id="step2_indicator">
            <div class="step_badge"><span class="step_number">2</span></div>
            <div class="step_label">登入</div>
        </div>
    </div>

    <div class="formWrap">
        <!-- Step 1: 輸入 BackupEmail -->
        <div id="step1_content">
            <div class="text_start_align mx-auto forgot_form">
                <label class="account d-flex align-items-center gap-2 mb-3">
                    輸入備援信箱 *
                </label>
                <div class="input_row">
                    <div class="input_group_custom flex-grow-1">
                        <input id="inputBackupemail" class="input_custom" type="email" placeholder="example@gmail.com" style="width: 100%;">
                    </div>
                    <button type="button" class="btn_send_verify" id="btn_send_reset" disabled>寄驗證信</button>
                </div>
                <div id="emailHint" class="inputHint mt-2"></div>

                <div class="alert_custom mt-4 d-none" id="custom_alert">
                    <i class="bi bi-bell fs-4"></i>
                    <div>
                        <strong>我們向您發送了一封驗證信。</strong>
                        <br>
                        <small class="text-muted">請點擊信中的連結驗證您的帳號。</small>
                    </div>
                </div>

                <!-- 原本的 resend_wrap 改為僅顯示提示 -->
                <div class="mt-3 text-center d-none" id="resend_wrap">
                    <div class="small text-muted mt-2" id="resend_hint"></div>
                </div>
                
                <!-- 下一步：需已完成驗證才可按 -->
                <button type="button" class="btn_next_step mt-4" id="btn_next_step" disabled>下一步</button>
            </div> <!-- ★ 補上這個缺失的 div (結束 forgot_form) -->
        </div> <!-- 結束 step1_content -->
        <div class="backEmailbtn">
       
        </div>
       
        <!-- Step 2: 完成 -->
        <div id="step2_content" class="d-none text-center">
            <div class="mb-5">
                <div class="display-1 text-success mb-3">✓</div>
                <h3>成功找回帳號：您的帳號是：</h3>
            </div>
    <button type="button" class="btn_next_step mt-3 btn_Gray" id="btnClearAndNew">重新註冊</button>
          
        </div>
    </div>
    <div class="forgotWrap"><a href="@Url.Action("Login", "Auth")" class="forgotEmailDisplay">回到登入</a>｜<a href="@Url.Action("ForgotPassword", "Auth")" class="forgotEmailDisplay">忘記密碼</a></div>

</div>

@section Scripts {
    <script src="~/js/AuthApi/forgotEmail.js" asp-append-version="true"></script>
}

